server:
  port: 8080

spring:
  config:
    import:
      - optional:file:.env[.properties]
      - optional:file:backend/.env[.properties]

  application:
    name: subastas-api

  datasource:
    url: jdbc:mysql://localhost:3306/subastas?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Argentina/Buenos_Aires
    username: root
    password: 1234r
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 12MB

app:
  jwt:
    secret: ${JWT_SECRET:clave-super-secreta-de-al-menos-32-bytes-para-HS256}
    access-token-minutes: 30
    refresh-token-days: 7

  cors:
    allowed-origins: "*"

  email:
    provider: ${MAIL_PROVIDER:dev}
    api-key: ${MAILTRAP_API_KEY:${RESEND_API_KEY:}}
    api-url: ${MAIL_API_URL:}
    from-email: ${MAIL_FROM:mailtrap@demomailtrap.co}
    from-name: ${MAIL_FROM_NAME:Bidster}
    test-to-email: ${MAIL_TEST_TO:}

logging:
  level:
    org.springframework.security: INFO
    com.subastas.api: DEBUG